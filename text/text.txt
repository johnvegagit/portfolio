/*
        const readMoreUls = document.querySelectorAll('.read-more-ul');
        
        // Restablecer la altura de todos los elementos
        readMoreUls.forEach(element => {
            element.style.height = "initial";
        });

        // Establecer la altura del elemento correspondiente al botón clicado a "auto"
        const parent = btn.parentNode; // Elemento padre del botón
        const readMoreUl = parent.querySelector('.read-more-ul');
        readMoreUl.style.height = "auto";
*/


/*
const readMorBtn = document.querySelectorAll('.read-more-btn');
readMorBtn.forEach(btn => {
    btn.addEventListener('click', () => {
        const readMoreUl = document.querySelectorAll('.read-more-ul');
        for (let i = 0; i < readMoreUl.length; i++) {
            const element = readMoreUl[i];
            element.style.height = "auto";
        }
    });
});/*

/*



const readMoreUl = document.querySelectorAll('.read-more-ul');
for (let i = 0; i < readMoreUl.length; i++) {
    const element = readMoreUl[i];
    const ul = element.getAttribute('data-ul');
    //console.log(ul);
}

document.querySelector('.read-more-btn').addEventListener('click', ()=> {
    console.log('ID:', ul);
});*/





//.style.height = "auto";
    //const ulRead = document.querySelector('.read-more-ul');
    /*if (parent.querySelector("ul").style.height === "100px")  {
        parent.querySelector("ul").style.height = "auto";
    } else {
        parent.querySelector("ul").style.height = "100px";
    }*/

      console.log(parent.querySelector('ul').clientHeight);